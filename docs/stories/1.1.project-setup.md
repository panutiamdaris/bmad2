# Story 1.1: Project Setup & Repository Structure

## Status
Approved

## Story
**As a** developer,  
**I want** a structured monorepo with frontend, backend, shared types, and documentation folders with initial build configuration,  
**so that** I have a consistent development environment and can begin implementing features with proper separation of concerns.

## Acceptance Criteria
1. Monorepo created with `/client` (React + TypeScript), `/server` (Node.js + Express), `/shared` (TypeScript interfaces), `/docs` folders
2. Package.json workspaces configured for dependency management across packages
3. TypeScript configured with strict mode and shared tsconfig for type consistency
4. ESLint and Prettier configured with consistent rules across client and server
5. Git repository initialized with .gitignore for node_modules, build artifacts, environment files
6. README.md documents folder structure, setup instructions, and development commands
7. Basic npm scripts defined for install, build, dev, and lint operations

## Tasks / Subtasks

- [ ] Initialize monorepo structure (AC: 1)
  - [ ] Create root directory `vehicle-tracker/`
  - [ ] Create `packages/web/` directory for Next.js frontend
  - [ ] Create `packages/api/` directory for Azure Functions backend
  - [ ] Create `packages/sync-engine/` directory for sync logic
  - [ ] Create `packages/sync-worker/` directory for background sync processor
  - [ ] Create `packages/shared/` directory for shared types and utilities
  - [ ] Create `packages/infrastructure/` directory for Azure Bicep files
  - [ ] Create `scripts/` directory for build and deployment scripts
  - [ ] Create `docs/` directory for project documentation
  - [ ] Create `.github/workflows/` directory for CI/CD pipelines

- [ ] Configure package manager and workspaces (AC: 2)
  - [ ] Create root `package.json` with pnpm workspaces configuration
  - [ ] Add `pnpm-workspace.yaml` defining package paths
  - [ ] Configure Turborepo in `turbo.json` for task orchestration
  - [ ] Create `package.json` for each workspace package (web, api, sync-engine, sync-worker, shared)
  - [ ] Define inter-package dependencies (web→shared, api→shared, etc.)

- [ ] Configure TypeScript (AC: 3)
  - [ ] Create root `tsconfig.json` with strict mode enabled
  - [ ] Add shared TypeScript config with paths for `@shared/*` aliases
  - [ ] Create `tsconfig.json` for each package extending base config
  - [ ] Configure TypeScript version 5.3.3 across all packages
  - [ ] Set up path mappings for cross-package imports

- [ ] Configure ESLint and Prettier (AC: 4)
  - [ ] Install ESLint 8.56.0 and Prettier 3.2.4 at root
  - [ ] Create `.eslintrc.js` with TypeScript and React rules
  - [ ] Create `.prettierrc` with single quotes, 2-space indent, 100 column width
  - [ ] Add `.eslintignore` and `.prettierignore` files
  - [ ] Configure ESLint to work with Prettier (no conflicts)
  - [ ] Add lint scripts to root and package-level package.json files

- [ ] Initialize Git repository (AC: 5)
  - [ ] Run `git init` in project root
  - [ ] Create `.gitignore` excluding node_modules, dist, build, .env files
  - [ ] Add additional ignores for IDE files, OS files, coverage reports
  - [ ] Configure Git LFS for large binary files (if needed)
  - [ ] Make initial commit with repository structure

- [ ] Create comprehensive README.md (AC: 6)
  - [ ] Document project overview and tech stack
  - [ ] Describe monorepo folder structure with purpose of each package
  - [ ] Provide setup instructions (pnpm install, environment setup)
  - [ ] List development commands (dev, build, test, lint)
  - [ ] Document package interdependencies and workspace architecture
  - [ ] Add troubleshooting section for common setup issues

- [ ] Configure npm scripts (AC: 7)
  - [ ] Add root-level `install` script for pnpm install
  - [ ] Add root-level `build` script using Turborepo to build all packages
  - [ ] Add root-level `dev` script to run development servers concurrently
  - [ ] Add root-level `lint` script to lint all packages
  - [ ] Add root-level `format` script to format all code with Prettier
  - [ ] Add root-level `clean` script to remove all node_modules and build artifacts
  - [ ] Add package-specific scripts (web:dev, api:dev, etc.)

- [ ] Verify setup and create smoke test (AC: 7)
  - [ ] Run `pnpm install` and verify all dependencies installed
  - [ ] Run `pnpm build` and verify all packages build without errors
  - [ ] Run `pnpm lint` and verify no linting errors
  - [ ] Create simple "Hello World" component in web package
  - [ ] Create simple "Hello World" function in api package
  - [ ] Verify Turborepo caching works correctly
  - [ ] Document verification steps in README

## Dev Notes

### Previous Story Insights
This is the first story in the project. No previous stories exist.

### Project Structure and Organization
**[Source: architecture/09-source-tree.md]**

The monorepo structure follows this exact organization:
```
vehicle-tracker/
 .github/workflows/     # GitHub Actions CI/CD
 packages/
  web/                  # Next.js 14.1.0 frontend with App Directory
   src/app/
   public/
   tests/
  sync-engine/          # Client-side sync logic (Dexie.js wrapper)
   src/
  api/                  # Azure Functions 4.x for main API
   src/functions/
   tests/
  sync-worker/          # Azure Functions for background sync processing
   src/functions/
  shared/               # Shared TypeScript types, schemas, utilities
   src/
    types/
    schemas/
    constants/
    utils/
  infrastructure/       # Azure Bicep IaC files
   main.bicep
   modules/
   parameters/
 scripts/               # Build and deployment automation
 docs/                  # Project documentation
 turbo.json            # Turborepo configuration
 pnpm-workspace.yaml   # pnpm workspace definition
 package.json          # Root package with workspace config
 tsconfig.json         # Base TypeScript configuration
```

**Key Package Dependencies:**
- web → shared, sync-engine
- sync-engine → shared
- api → shared
- sync-worker → shared
- shared → none (no dependencies on other packages)

### Technology Versions and Tools
**[Source: architecture/03-tech-stack.md]**

**Required Versions:**
- TypeScript: 5.3.3
- Node.js: 20.11.0 LTS
- pnpm: 8.15.0 (package manager)
- Turborepo: 1.12.0 (monorepo task runner)
- React: 18.2.0
- Next.js: 14.1.0
- Azure Functions: 4.x
- Dexie.js: 3.2.4
- Vite: 5.0.12 (if needed for non-Next.js builds)
- Vitest: 1.2.0 (unit testing)
- Playwright: 1.41.0 (E2E testing)
- ESLint: 8.56.0
- Prettier: 3.2.4
- Zustand: 4.5.0 (state management)
- Zod: 3.22.4 (validation)
- date-fns: 3.2.0
- Azure Bicep: 0.24.24

**Rationale for Key Choices:**
- pnpm for disk efficiency and fast installs in monorepo
- Turborepo for build caching and parallel task execution
- Next.js 14 with App Directory for modern React patterns and PWA support
- Azure Functions for serverless backend (cost-effective, scales to zero)
- Dexie.js for powerful IndexedDB queries in offline-first architecture

### Coding Standards to Apply
**[Source: architecture/12-coding-standards.md]**

**File Naming:**
- All files use kebab-case: `sync-manager.ts`, `vehicle-card.tsx`
- React components: PascalCase for component, kebab-case for file: `VehicleCard` in `vehicle-card.tsx`

**TypeScript Configuration:**
- Strict mode MANDATORY
- No `any` type usage (prefer `unknown` + type guards)
- Explicit return types on public functions
- Use `interface` for object shapes
- Use `readonly` for immutable props

**ESLint & Prettier:**
- Single quotes
- 2-space indentation
- 100 character line width
- Trailing commas in multi-line
- No semicolons (Prettier default)

**Constants:**
- Use SCREAMING_SNAKE_CASE for constants: `MAX_RETRIES`, `API_BASE_URL`

**Error Handling:**
- Never use console.log in production
- All async functions must have try/catch blocks
- Use error code constants (no hardcoded error strings)

**Security:**
- No secrets in code (use environment variables)
- Add .env files to .gitignore
- Validate all inputs with Zod schemas

### Testing Standards
**[Source: architecture/13-test-strategy-and-standards.md]**

**Testing Framework Setup:**
- Unit Tests: Vitest 1.2.0
- Integration Tests: Vitest with Testcontainers
- E2E Tests: Playwright 1.41.0

**Test File Organization:**
- Unit tests: Colocated with source files as `{filename}.test.ts`
- Integration tests: `packages/{package}/tests/integration/`
- E2E tests: `tests/e2e/` at project root

**Coverage Requirements:**
- Overall coverage goal: 80%+
- Test pyramid: 70% Unit, 20% Integration, 10% E2E

**For this story specifically:**
- Create smoke tests verifying build and lint scripts work
- Test that workspace dependencies resolve correctly
- Test that TypeScript compilation succeeds across packages

**CI/CD Integration:**
- All tests run on every PR
- Build fails if coverage < 80% (enforced later in pipeline)

### Git Configuration
**.gitignore must include:**
```
node_modules/
dist/
build/
.next/
.turbo/
.env
.env.local
.env.*.local
*.log
coverage/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
```

### Environment Variables Setup
**For this initial story:**
- Create `.env.example` file at root with placeholder variables
- Document that actual `.env` file must be created locally (not committed)
- Add `.env` to .gitignore

**Example variables for later stories:**
```
# Azure Configuration (to be filled in Story 1.2)
AZURE_COSMOS_CONNECTION_STRING=
AZURE_BLOB_STORAGE_CONNECTION_STRING=
AZURE_SERVICE_BUS_CONNECTION_STRING=

# Authentication (to be filled in Story 1.4)
JWT_SECRET=
JWT_REFRESH_SECRET=

# Application
NODE_ENV=development
API_BASE_URL=http://localhost:7071
```

### Workspace Configuration Details

**pnpm-workspace.yaml structure:**
```yaml
packages:
  - 'packages/*'
```

**Root package.json key sections:**
```json
{
  "name": "vehicle-tracker",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "pnpm install",
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^1.12.0",
    "typescript": "^5.3.3",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4"
  }
}
```

**turbo.json configuration:**
```json
{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "clean": {
      "cache": false
    }
  }
}
```

### README.md Structure
The README must include:
1. Project title and description
2. Tech stack overview (link to full architecture docs)
3. Prerequisites (Node.js 20.11.0, pnpm 8.15.0)
4. Installation instructions
5. Development workflow
6. Folder structure explanation
7. Package interdependencies diagram/description
8. Common commands reference
9. Troubleshooting section
10. Contributing guidelines (if applicable)

## Testing

### Test File Location
**[Source: architecture/13-test-strategy-and-standards.md]**
- Unit tests colocated with source: `{filename}.test.ts`
- Integration tests: `packages/{package}/tests/integration/`
- E2E tests: `tests/e2e/`

### Testing Framework
- Framework: Vitest 1.2.0
- Test runner configured in each package's `package.json`
- Use AAA pattern (Arrange, Act, Assert)

### Testing Requirements for This Story
Since this is infrastructure setup, testing focuses on verification:
1. **Workspace Resolution Test:** Verify that imports from `@shared` resolve correctly in web and api packages
2. **Build Test:** Verify all packages build successfully with `pnpm build`
3. **Lint Test:** Verify ESLint and Prettier run without errors
4. **TypeScript Compilation Test:** Verify strict mode TypeScript compiles across all packages
5. **Script Execution Test:** Verify all npm scripts execute without errors

### Smoke Test Scenarios
- Create minimal TypeScript file in shared package exporting a type
- Import that type in web package and verify it resolves
- Import that type in api package and verify it resolves
- Run build and confirm all packages compile
- Run lint and confirm no errors
- Verify Turborepo caching by running build twice (second should be cached)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-09 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record
*This section will be populated by the development agent during implementation.*

### Agent Model Used
_To be filled by dev agent_

### Debug Log References
_To be filled by dev agent_

### Completion Notes List
_To be filled by dev agent_

### File List
_To be filled by dev agent_

## QA Results
*This section will be populated by QA Agent after implementation review.*
